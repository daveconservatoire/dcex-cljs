(ns daveconservatoire.server.core-tests
  (:require [cljs.test :refer-macros [deftest is are run-tests async testing run-tests]]
            [daveconservatoire.server.core]))

(deftest sample-test
  (is (= 2 2)))

(deftest test-api-courses
  )
